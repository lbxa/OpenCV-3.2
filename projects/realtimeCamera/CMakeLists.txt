cmake_minimum_required(VERSION 2.8)
project( RealtimeCamera )
find_package( OpenCV REQUIRED )
add_executable( RealtimeCamera realtimeCamera.cpp )
target_link_libraries( RealtimeCamera ${OpenCV_LIBS} )